import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <p className="App-intro">
                Burndown with <code>Issues & Milestones</code> from Github.
            </p>
        );
    }
}

export default Header;